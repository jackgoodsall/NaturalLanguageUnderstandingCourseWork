# Solo Solution C Scaffold

This directory is reserved for a fresh solo implementation of `Solution C`.

Scope:

- transformer-based model path
- independent of the existing team `Solution C` notebooks
- built from the coursework data in `training_data/`

Suggested first files:

- `config.py`
- `data.py`
- `train.py`
- `evaluate.py`
- `inference.py`

The goal on this branch is to rebuild the transformer path cleanly from scratch.
